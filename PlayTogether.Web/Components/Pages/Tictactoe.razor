@page "/tictactoe"
<h3>Tic-Tac-Toe Game</h3>
<div class="spiel-flaeche">
    @for (int reihe = 0; reihe < 3; reihe++)
    { reihefest = reihe;
        reihefest2 = reihefest;
        <div class="bord-reihe">
            @for (int spalte = 0; spalte < 3; spalte++)
            {spaltefest = spalte;
                spaltefest2 = spaltefest;
                <button class="zelle"@onclick="() => MakeMove(reihe, spalte)">
                    @board[reihe, spalte]
                </button>
            }
        </div>
    }
</div>

@code{
    private string currentplayer = "X";
    private bool WasLastMoveX = false;
    private string[,] board = new string[3, 3];
    private int spaltefest2;
    private int reihefest2;
    private int reihefest;
    private int spaltefest;
    private void MakeMove(int reihe, int spalte)
    {


        
           if(WasLastMoveX = false)
            {
                board[reihefest2, spaltefest2] = "X";
                WasLastMoveX = true; // Wechsel zu O
            }
            
            if(WasLastMoveX = true)
            {
                board[reihefest2, spaltefest2] = "O";
                WasLastMoveX = false; // Wechsel zu X
            }
        
    }

    
}
<style>
    .spiel-flaeche {
        display: flex;
        flex-direction: column;
        align-items: center;
        gap: 10px;
    }

    .bord-reihe {
        display: flex;
        gap: 10px;
    }

    .zelle {
        width: 90px;
        height: 90px;
        font-size: 2rem;
        background-color: #f0f0f0;
        border: 2px solid #333;
        border-radius: 8px;
        cursor: pointer;
        display: flex;
        justify-content: center;
        align-items: center;
        font-weight: bold;
       
    }
    .zelle.x {
        font-size: 4rem;    
        font-weight: bold;  
        color: red;         
        display: flex;
        justify-content: center; 
        align-items: center;     
    }

   
    .zelle.o {
        font-size: 4rem;    
        font-weight: bold;  
        color: blue;        
        display: flex;
        justify-content: center;  
        align-items: center;    
    }

</style>


